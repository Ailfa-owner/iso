# الإصلاحات في Alif Linux 0.1.1 VMware Fix AMD64

تم إنشاء هذه النسخة لمعالجة مشكلتين في الإصدار السابق:

1. تم قفل كلمة مرور المستخدم الافتراضي `alif` بوضع `!` في `/etc/shadow`. لم تعد كلمة المرور الفارغة صالحة للمصادقة.
2. تمت إضافة الحزمتين `open-vm-tools` و`open-vm-tools-desktop`، مع تفعيل `open-vm-tools.service` داخل `multi-user.target`. أصبح `vmtoolsd` موجوداً في الصورة، ما يوفر تكامل VMware الخاص بمزامنة الوقت وخدمات الضيف، وتكامل سطح المكتب مثل تغيير الدقة والحافظة بحسب إعدادات VMware والبيئة الرسومية.

## التحقق

البصمة SHA-256 للصورة الكاملة:

```text
10065a5fff5477d2d8557b16f2d152cf2e424238b1450c2d2798f6e93ee0cccc  alif-linux-0.1.1-vmware-fix-amd64.iso
```

أُعيد بناء الصورة مع إقلاع BIOS وUEFI، ونجح فحص جميع قطاعات ISO بواسطة `xorriso` بجودة `+ good`.

## ملاحظة أمنية

قفل كلمة المرور يمنع تسجيل الدخول بكلمة مرور فارغة. لا يزال من الضروري إنشاء كلمة مرور شخصية أثناء تثبيت النظام، وتعطيل أي دخول عن بعد غير مطلوب، وتثبيت تحديثات الأمان دورياً.
