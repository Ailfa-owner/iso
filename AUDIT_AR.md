# تقرير تدقيق Alif Linux 0.1.0 VMware Fix AMD64

## نطاق الفحص

أُجري الفحص على الملف `alif-linux-0.1.0-vmware-fix-amd64.iso` دون تثبيته على جهاز فعلي ودون تعديل محتواه. شمل الفحص بنية ISO 9660، سجلات الإقلاع El Torito، إقلاع BIOS وUEFI، ملفات GRUB، طبقة SquashFS، إعدادات الحسابات، مصادر APT، الخدمات والسكربتات المخصصة، والمؤشرات المعروفة للأسرار أو الملفات التنفيذية غير المعتادة.

## معلومات الصورة

| العنصر | النتيجة |
|---|---|
| الحجم | 2,928,277,504 بايت تقريباً (2.8 GiB) |
| نوع الملف | ISO 9660، قابل للإقلاع، DOS/MBR boot sector |
| Volume ID | `ALIF_LINUX` |
| البصمة SHA-256 | `a1465452add4896962dda3a3ec0092444e9ab9a02cb8cd420e96846db2eca290` |
| النظام المعلن | Alif Linux 0.1، مبني على Ubuntu 24.04 LTS |
| النواة | `6.8.0-138-generic` |
| الملفات | 591 ملفاً مستعاداً من ISO، مع 600 عقدة في شجرة الصورة |
| النظام المضغوط | SquashFS 4.0، ضغط Zstandard، حوالي 2.7 GiB مضغوطاً و7.1 GB بعد الفك |
| الإقلاع | BIOS وUEFI موجودان في كتالوج El Torito |
| فحص القطاعات | نجح: كل 1,429,823 قطاعاً أُبلغ عنها بجودة `+ good` |

## العيوب والمخاطر المؤكدة أو المرجحة

### 1. حساب المستخدم الافتراضي بلا كلمة مرور — خطورة عالية بعد التثبيت

يظهر الحساب `alif` في `/etc/shadow` بحقل كلمة مرور فارغ:

```text
alif::20692:0:99999:7:::
```

وفي الوقت نفسه لديه shell تفاعلي `/bin/bash`، كما أن إعدادات sudo العامة تسمح لأعضاء مجموعة `sudo` بتنفيذ أي أمر بصلاحيات root. قد يكون ذلك مقصوداً لجلسة Live، لكنه خطر إذا انتقلت الحالة نفسها إلى نظام مثبت أو إذا فُعّل تسجيل دخول محلي/بعيد دون فرض إنشاء كلمة مرور.

**الإصلاح المقترح:** قفل الحساب أو إعطاؤه كلمة مرور عشوائية مؤقتة في Live، وعدم السماح بكلمة مرور فارغة في المثبّت، وإجبار المستخدم على إنشاء كلمة مرور أثناء التثبيت، وتعطيل SSH أو تقييده افتراضياً.

### 2. لا يوجد تكامل VMware واضح رغم اسم الإصدار

يوجد تعريف X.Org `xserver-xorg-video-vmware`، لكن الفحص لم يجد حزم `open-vm-tools` أو ملفات/خدمات VMware التشغيلية. وجود حزمة تعريف X.Org وحدها لا يضمن clipboard أو resize أو time synchronization أو مشاركة الملفات في VMware.

**الإصلاح المقترح:** إضافة واختبار `open-vm-tools` و`open-vm-tools-desktop` عند الحاجة، تفعيل الخدمة المناسبة، وتوثيق إعدادات VMware الموصى بها. يجب اختبار Unity/Wayland/3D acceleration ونسخ النص وتغيير الدقة داخل VMware Workstation/Player، لا الاكتفاء بوجود التعريف.

### 3. اختبار الإقلاع الرسومي غير محسوم آلياً

أُجري تشغيل QEMU لمدة 90 ثانية مع `-display none` وserial console. لم يظهر kernel panic أو رسالة فشل، لكن العملية بقيت تعمل حتى انتهت المهلة؛ وهذا متوقع جزئياً لأن GRUB يمرر الإقلاع إلى جلسة رسومية ولا يضيف `console=ttyS0`. لذلك لا يمكن من هذا الاختبار وحده إثبات نجاح سطح المكتب أو فشل VMware.

**الإصلاح المقترح:** إضافة وضع تشخيص قابل للتسجيل يمرر `console=ttyS0` أو توفير خيار `nomodeset` مع تسجيل واضح، ثم اختبار الصورة بصرياً في QEMU وVMware فعلياً.

### 4. عدم وجود فحص سلامة منشور داخل الإصدار الأصلي

الصورة تحتوي على `casper/filesystem.squashfs` وملفات الإقلاع، لكن لا يظهر ملف checksums منشور للمستخدم داخل ISO. هذا يجعل التحقق بعد التنزيل أصعب ويزيد احتمال استخدام ملف تالف دون اكتشافه.

**الإصلاح المقترح:** نشر `SHA256SUMS` موقّعاً أو على الأقل مرفقاً مع كل إصدار، وتوفير توقيع GPG أو Sigstore للإصدارات المهمة.

### 5. الهوية والمعلومات التعريفية غير مكتملة

`/etc/os-release` يعرّف النظام باسم Alif Linux 0.1، لكنه يحتوي على تعليق يفيد بأن روابط المشروع الرسمية ستُضاف لاحقاً. كما أن الصورة تعرض اسم إصدار أولي فقط ولا تحتوي على معلومات واضحة عن تاريخ البناء أو commit المصدر.

**الإصلاح المقترح:** إضافة `HOME_URL` و`SUPPORT_URL` و`BUG_REPORT_URL`، وملف build-info يتضمن commit، تاريخ البناء، نسخة الأدوات، ومصدر الحزم.

### 6. سكربتات إدارة الحزم تحتاج تشديداً واختبارات

`alif-update` يستخدم `pkexec` ثم ينفذ تحديثات وترقية وإزالة حزم. توجد آلية قفل في `alif-transaction`، لكنها لا تحمي مسار `alif-repair` الذي ينفذ عمليات `dpkg` وAPT مباشرة. كما أن `alif-repo-check` يتحقق من أسماء توزيعات محددة فقط ولا يثبت أن المستودع موثوق أو أن HTTPS مستخدم.

**الإصلاح المقترح:** توحيد جميع عمليات APT خلف قفل واحد، استخدام HTTPS، التحقق من صلاحية مصادر APT، تسجيل الأخطاء للمستخدم، وإضافة اختبارات لحالات انقطاع الشبكة وتزامن عمليتي تحديث.

### 7. حجم الصورة كبير بالنسبة لصورة Live

حجم ISO حوالي 2.8 GiB، والنظام بعد فك SquashFS يقارب 7.1 GB. هذا يزيد وقت التنزيل والإقلاع ومتطلبات التخزين، وقد يكون غير مناسب لبعض وسائط USB أو البيئات الافتراضية ذات الموارد المحدودة.

**الإصلاح المقترح:** إزالة حزم التطوير واللغات غير المطلوبة من صورة Live، فصل صورة minimal عن صورة desktop، وفحص التكرار والملفات الكبيرة قبل كل إصدار.

## نقاط إيجابية

الصورة قابلة للقراءة والاستخراج، وفهرس ISO سليم، وفحص جميع القطاعات بواسطة `xorriso` نجح. كما أن الصورة تحتوي على مساري BIOS وUEFI، وتوفر خيار safe graphics بخيار `nomodeset`، وتحتوي على أدوات مخصصة لفحص النظام وسياسة APT تمنع بعض أسماء إصدارات Debian/Ubuntu غير المتوافقة.

## حدود التدقيق

هذا تدقيق ثابت ومحدود ببيئة sandbox. لم يتم تثبيت النظام على قرص، ولم يتم اختبار كل الأجهزة أو الشبكات أو الرسوميات ثلاثية الأبعاد أو VMware Workstation/Player مباشرة. كما أن استخراج SquashFS كمستخدم غير root لم يستطع إنشاء عقد أجهزة `/dev`، وهو قيد لأداة الاستخراج وليس دليلاً بحد ذاته على عيب في ISO. لم يظهر أثناء الفحص مؤشر واضح على مفتاح خاص خاص بالمشروع أو حمولة ضارة، لكن هذا لا يساوي تدقيقاً أمنياً كاملاً أو ضماناً بعدم وجود ثغرات.

## توصية الإصدار

لا يُنصح بتثبيت هذه النسخة على جهاز دائم أو استخدامها على شبكة موثوقة قبل معالجة الحساب ذي كلمة المرور الفارغة، وتوثيق/اختبار تكامل VMware، ونشر checksums موقعة، واختبار الإقلاع الرسومي والتثبيت على VMware وQEMU وعتاد حقيقي.
